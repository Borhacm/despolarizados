/** Clave de localStorage para preferencia claro/oscuro (sin prefijo para no chocar con otros orígenes). */
export const THEME_STORAGE_KEY = "despolarizados-theme";

export type ThemeChoice = "light" | "dark";
